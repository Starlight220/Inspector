package io.starlight

/**
 * There are two projects here:
 *
 * - The `io.starlight.env` package deals with interacting with (and mocking) the GitHub Actions
 * environment. This package might be split to a separate module later.
 *
 * - The `io.starlight.inspector` package is the implementation of Inspector.
 */
